package Interface;

public interface Circle{
    float pi=3.14f;     // by default public, final, static
    void calculateArea();
}

// By default, all methods are public and abstract until we do not declare it as default and properties are static and final.